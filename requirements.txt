# Alpharidge AI API Dependencies

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database
prisma>=0.11.0

# Pydantic for data validation
pydantic>=2.5.0

# Bittensor for authentication
bittensor>=10.0.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client (for internal calls if needed)
httpx>=0.25.0

# Numeric operations (embedding cosine similarity)
numpy>=1

# Async support: asyncio is part of the Python 3 standard library. Do NOT install the
# PyPI `asyncio` backport (3.4.3, from 2015) — on modern Python it shadows and breaks
# the stdlib module. Intentionally removed.
# asyncio>=3.4.3

pytest>=7.4.0
pytest-asyncio>=0.23.0

